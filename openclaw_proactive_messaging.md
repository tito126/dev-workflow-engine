# OpenClaw 主动消息推送能力

## 概述

OpenClaw 可以主动向用户发送消息，无需用户触发。这为自动化任务、定时提醒、异常告警等场景提供了强大的支持。

---

## ✅ 测试验证

**测试时间**：2026-03-02 18:35
**测试结果**：✅ 成功发送主动消息
**消息 ID**：om_x100b5562cebcc4b0c429859bc9963b1

---

## 📨 应用场景

### 1. 任务完成通知

当长时间任务（如日志巡检）完成后，主动推送结果：

```
桐乡病区护士站日志分析完成！
发现 196 条错误，98 个慢接口。
报告已生成：report_txwsjkj_2026-03-02.html
```

**优势**：

- 用户无需等待或轮询
- 及时获取结果
- 提升用户体验

---

### 2. 定时提醒

设置定时任务，在特定时间主动发送提醒：

```
早上好！昨天有 3 个医院的日志需要巡检，是否现在开始？
```

**应用场景**：

- 每日工作提醒
- 周报/月报推送
- 待办事项提醒
- 会议提醒

---

### 3. 异常告警

监控系统状态，发现异常立即推送：

```
⚠️ 紧急：乐山市人民医院费用服务不可用，已连续失败 108 次！
```

**应用场景**：

- 服务异常告警
- 错误日志激增
- 慢接口超时
- 系统资源告警

---

### 4. 进度更新

长时间任务的实时进度反馈：

```
正在分析日志... 已完成 50% (10000/20000 条)
```

**应用场景**：

- 大文件处理
- 批量数据分析
- 长时间计算任务

---

### 5. 定时报告

定期生成并推送汇总报告：

```
📊 上周日志巡检汇总：
- 共巡检 15 次
- 发现 42 个问题
- 已修复 38 个
```

**应用场景**：

- 日报/周报/月报
- 统计分析
- 趋势报告

---

## 🔧 技术实现

### 方式 A：命令行调用

```bash
openclaw message send \
  --channel feishu \
  --target ou_9e60c9c7a82eab3887d7ba8da7741cfb \
  --message "你的消息内容"
```

**适用场景**：

- 脚本中调用
- 手动发送
- 简单通知

---

### 方式 B：Python 脚本集成

```python
import subprocess

def send_notification(message):
    """发送飞书通知"""
    subprocess.run([
        'openclaw', 'message', 'send',
        '--channel', 'feishu',
        '--target', 'ou_9e60c9c7a82eab3887d7ba8da7741cfb',
        '--message', message
    ])

# 使用示例
send_notification("日志分析完成！")
```

**适用场景**：

- 在日志巡检脚本中集成
- 任务完成后自动通知
- 异常捕获后告警

---

### 方式 C：Cron 定时任务

```bash
# 每天早上 9 点发送提醒
openclaw cron add \
  --schedule "0 9 * * *" \
  --task "openclaw message send --channel feishu --target ou_9e60c9c7a82eab3887d7ba8da7741cfb --message '早上好！今天需要巡检的医院：桐乡、乐山'"
```

**Cron 表达式说明**：

- `0 9 * * *` - 每天 9:00
- `0 */2 * * *` - 每 2 小时
- `0 9 * * 1` - 每周一 9:00
- `0 9 1 * *` - 每月 1 号 9:00

**适用场景**：

- 定时提醒
- 定期报告
- 自动巡检

---

### 方式 D：Agent 自动发送

Agent 可以在任何时候根据逻辑判断主动发送消息：

```python
# Agent 内部逻辑（伪代码）
if error_count > 100:
    send_alert("⚠️ 错误日志激增！")

if task_completed:
    send_notification("✅ 任务完成！")

if is_morning():
    send_reminder("早上好！")
```

**适用场景**：

- 智能判断
- 条件触发
- 复杂业务逻辑

---

## 💡 对日志巡检项目的意义

### 完整的自动化流程

```
步骤 1: 用户发起请求
用户："帮我巡检桐乡病区护士站今天上午的日志"

步骤 2: 立即响应
机器人："收到！开始分析桐乡病区护士站 08:00-12:00 的日志..."

步骤 3: 后台执行
[执行日志拉取、分析、报告生成，可能需要 5-10 分钟]

步骤 4: 主动推送结果
机器人："✅ 分析完成！
- 总日志：20000 条
- 错误：196 条
- 警告：19784 条
- 慢接口：98 个
报告已生成，正在发送..."

步骤 5: 发送报告
[发送 HTML 报告文件或链接]
```

---

### 定时自动巡检

```
配置定时任务：
- 每天早上 8:00 自动巡检所有医院
- 发现问题主动推送告警
- 无需人工触发
- 全自动运维
```

**实现方式**：

```bash
# 创建定时巡检任务
openclaw cron add \
  --schedule "0 8 * * *" \
  --task "python /path/to/auto_inspect.py"
```

```python
# auto_inspect.py
hospitals = ["桐乡", "乐山", "浙江省中"]

for hospital in hospitals:
    result = run_inspection(hospital)
    if result.has_errors():
        send_alert(f"⚠️ {hospital} 发现 {result.error_count} 个问题！")
    else:
        send_notification(f"✅ {hospital} 巡检正常")
```

---

### 异常实时告警

```
监控规则：
- 错误日志 > 100 条/小时 → 立即告警
- 慢接口 > 50 个 → 立即告警
- 服务不可用 → 立即告警
```

**实现方式**：

```python
# 在日志分析脚本中
if error_count > 100:
    send_alert(f"""
⚠️ 紧急告警！
医院：{hospital}
服务：{service}
错误数：{error_count}
时间：{time_range}
请立即处理！
    """)
```

---

## 📋 最佳实践

### 1. 消息分级

- **紧急**：⚠️ 红色，立即推送
- **重要**：⚡ 黄色，优先推送
- **普通**：ℹ️ 蓝色，正常推送
- **提示**：💡 灰色，低优先级

### 2. 避免消息轰炸

```python
# 设置消息频率限制
last_alert_time = {}

def send_alert_with_limit(key, message, min_interval=300):
    """最小间隔 5 分钟"""
    now = time.time()
    if key in last_alert_time:
        if now - last_alert_time[key] < min_interval:
            return  # 跳过，避免频繁推送
  
    send_notification(message)
    last_alert_time[key] = now
```

### 3. 消息格式规范

```
标题：简洁明了
内容：结构化
操作：提供下一步建议

示例：
✅ 日志巡检完成

医院：桐乡市卫生健康局
服务：病区护士站
时间：2026-03-02 08:00-10:00

结果：
- 错误：196 条
- 警告：19784 条
- 慢接口：98 个

报告：report_txwsjkj_2026-03-02.html

建议：
1. 优化慢接口
2. 减少日志输出
3. 检查错误原因
```

### 4. 支持交互

```
发送消息后，用户可以回复：
- "查看详情" → 发送完整报告
- "忽略" → 标记为已读
- "处理" → 记录处理状态
```

---

## 🎯 参赛项目应用

### 演示场景

**场景 1：手动触发**

```
用户："帮我巡检桐乡病区护士站今天上午的日志"
机器人："收到！开始分析..."
[5分钟后]
机器人："✅ 分析完成！发现 196 个错误..."
```

**场景 2：定时自动**

```
[每天早上 8:00]
机器人："早上好！开始今日自动巡检..."
[30分钟后]
机器人："📊 今日巡检完成！
- 桐乡：✅ 正常
- 乐山：⚠️ 发现 108 个错误
- 浙江省中：✅ 正常"
```

**场景 3：异常告警**

```
[实时监控]
机器人："⚠️ 紧急！乐山费用服务不可用！"
用户："详情"
机器人："[发送详细报告]"
```

---

## 📝 总结

OpenClaw 的主动消息推送能力为日志巡检项目提供了：

1. ✅ **完整的自动化闭环**：从触发到结果推送
2. ✅ **定时自动巡检**：无需人工干预
3. ✅ **实时异常告警**：第一时间发现问题
4. ✅ **进度实时反馈**：用户体验更好
5. ✅ **定期报告推送**：数据驱动决策

这使得日志巡检从"被动查询"变为"主动运维"，大大提升了效率和用户体验。

---

**文档生成时间**：2026-03-02 18:38
**测试状态**：✅ 已验证
**项目阶段**：参赛准备中
