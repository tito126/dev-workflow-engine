# 需求澄清 - 1554774

> 来源：TFS 1554774 | 集合：WINNING-6.0 | 项目：WiNEX-Inpatient-2
> 状态：已建议 | 分配：未分配
> 关联前序：1540718（同接口优化的补充需求）
> 澄清时间：2026-04-13 18:xx

---

## 需求澄清

### 类型
**性能优化**，属于既有优化方案的补充修正。

### 背景
- 同一接口：`/api/v1/execution_aggregate/ipt/cli_order_exec_result/query/by_example`
- 同一代码入口：`com.winning.ward.order.api.rest.ExecPlanRestImpl#queryOrderExecPlanResults`
- 1540718 已经围绕该接口做过一轮系统侧兜底优化，并落地过 `UNION -> UNION ALL` 的最小改动。
- 1554774 在 TFS 中给出的补充方向变为：
  1. `union` 修改成 `or`
  2. 建议新增索引 `IDX_EXEC_PLN_FLOW_ID_DEL_AT`
- 这说明补充需求正在**修正上一轮优化方向**，而不是普通并列小改。

### 目标
- 确认 1554774 是否意味着要回退 / 改写 1540718 的实现方向。
- 基于当前代码证据，判断本轮是否能直接进入实现。
- 如果能实现，优先走最小改动，并保证结果语义不被误伤。

### 非目标
- 不做无边界扩扫。
- 不默认扩大到前端仓。
- 不在未核实语义前直接把 `UNION ALL` 改成 `OR`。
- 不默认落地索引脚本，除非代码与约束都支持。

### 范围
- 仓库：`E:\winning-code\akso5\winning-nis-ward`
- 模块：住院护士站后端，`ward.order` 相关查询链
- 首扫根来源：YAML `backend_main`

### 成功标准
1. 确认 1554774 与 1540718 的真实差异点。
2. 精确收窄到本轮可能改动的文件与 SQL 结构。
3. 明确本轮允许改动层级：SQL 拼接 / 查询结构 / 索引脚本，哪些能动、哪些不能动。
4. 如果进入实现，改动应有可验证的代码证据，且不静默改变结果语义。

### 允许改动层级 / 禁止改动层级
- **允许候选**：后端查询结构、小范围 SQL 拼接调整、与该需求强相关的仓内代码。
- **待确认后才允许**：是否引入 / 调整索引脚本，是否把 `UNION ALL` 改成 `OR`。
- **禁止**：接口协议、无关模块、跨仓随意扩扫。

### 建议验收方式 / 衡量口径
- 代码完成：文件级改动和 SQL 结构变化可核对。
- 需求完成：补充需求要求的优化方向被准确落实。
- 效果已验证：需依赖真实环境 SQL plan / 耗时数据，当前阶段默认只能先做到代码证据。

### 外部依赖 / 前置条件
- 需要确认本轮是否以 TFS 补充口径为准，覆盖 1540718 的原实现方向。
- 需要确认是否允许新增索引。

### 风险 / 模糊点
1. `OR` 与 `UNION ALL` 的语义和执行计划可能并不等价。
2. TFS 给出的索引名与 1540718 中“DBA 不建议新增索引”的旧约束存在冲突，需重新核实。
3. 本轮属于 SQL / 性能优化补充任务，按护士站规则，分析后仍需显式确认允许采用的优化方向。

---

## 连续执行判断

### 任务级别
**medium**

### 是否值得进入持续工作流
**是**，但可压缩沿用 1540718 的已有上下文与产物，不必重走完整重型流程。

### 推荐执行路径
```text
brainstorming（本阶段 ✓）
  → writing-plans（压缩版）
  → locator（必须，确认补充需求实际影响）
  → user-confirmation gate（确认 OR / 索引是否放行）
  → implementer
  → verification
  → review-gate
```

### 是否可进入计划
**是**

### 是否可进入执行
**否**，还需先完成补充需求的代码定位，并确认本轮允许的优化方向。
