# 2026-03-18 (Wednesday)

## 日志巡检 - 乐山 K8s 链路打通

### 已确认结论

1. **乐山 K8s 拉取链路已打通**
   - 可通过运行时打开 `wxp-tunnel` / Lockjaw TCP 通道访问 Grafana NodePort `31003`
   - 成功路径是通过 Grafana/Loki 直接拉取，而不是修改主流程配置硬编码端口
   - 实际可用过的入口包括：
     - `http://172.16.7.39:8000`
     - `http://127.0.0.1:14828`

2. **两阶段完整链路逻辑已再次验证**
   - 正确流程仍然是：`fetch -> analyze -> fetch2 -> analyze -> report`
   - 第一阶段先拉 `ERROR|业务处理耗时`
   - `analyze` 产出 `*_digest_traces.json`
   - 第二阶段再按代表 `traceId` 拉完整链路（含 INFO）并 append 回原始 `.log`
   - 然后重新 `analyze` 再 `report`

3. **乐山最近 10 分钟曾完整跑通一次**
   - 时间窗：`2026-03-18 14:46:00 ~ 2026-03-18 14:56:00`
   - 产物：
     - `skills/log-inspect/logs_20260318_leshan_last10m_v2.log`
     - `skills/log-inspect/logs_20260318_leshan_last10m_v2_digest.json`
     - `skills/log-inspect/logs_20260318_leshan_last10m_v2_digest_traces.json`
     - `skills/log-inspect/logs_20260318_leshan_last10m_v2_report.html`

## 两个问题的定位

### 1. 基本信息里的“拉取耗时”没显示
- 不是模板坏了
- 原因是手工分阶段执行时，`analyze` 阶段漏传了 `--fetch-duration`
- 导致最终 digest 中：
  - `fetch_range.start/end` 有值
  - `fetch_range.duration_s` 是 `null`
- 因此报告里“拉取时间范围”仍显示，但“拉取耗时”消失

### 2. `be25d207c0c04053a3a54d7df3cc271c` 的“时间间隔分析”没显示
- 不是 HTML 渲染问题
- 原因是本地二阶段补链路不完整，少了 2 条日志
- 现场环境对比可见本地少 2 条，用户已确认“之前是可以拉取到的”

## 单 trace 验证结果

### trace: `be25d207c0c04053a3a54d7df3cc271c`
- `±30s`：只拉到 `12` 条
- `±60s`：拉到 `14` 条
- `±90s`：仍是 `14` 条

### 补回的关键 2 条时间点
- `2026-03-18 14:50:44,221`
- `2026-03-18 14:50:44,225`

### 结论
- 这类慢接口 trace 的默认二阶段窗口 `±30s` 偏小
- `±60s` 可以补齐已知缺失日志
- 问题核心不是“报告不展示”，而是“二阶段本地少拉了 2 条”

## 已实施代码改动

### 变更内容
- 将二阶段完整链路默认窗口从 `30s` 调整为 `60s`

### 变更文件
- `skills/log-inspect/log_inspect_main.py`
- `skills/log-inspect/loki_fetcher.py`

### 提交记录
- commit: `ad251c5`
- message: `Increase stage2 trace fetch window to 60s`

## 继续跑乐山最近 10 分钟时的状态

### 已完成
- 已重新打开乐山 Grafana 通道
- 已完成第一阶段拉取：
  - 时间窗：`2026-03-18 15:35:00 ~ 2026-03-18 15:45:00`
  - 原始日志文件：`skills/log-inspect/logs_20260318_154555_默认集群.log`
  - 日志量：`123085`
  - `RESULT_FETCH_DURATION: 87`

### 说明
- 这说明“拉取耗时”元数据本次已经拿到了
- 只要继续正确串后续 `analyze -> fetch2 -> analyze -> report`，报告中的“拉取耗时”应可恢复
- 但执行过程中 OpenAI 模型多次断开，导致流程中断在第一阶段完成后

## 红房子日志拉取（17:00 完成）

### 环境信息
- 租户代码：`fddxfsfckyy`
- Grafana 节点 IP：`172.168.16.65:31003`
- Lockjaw 隧道：`172.16.7.39:8012`（channelId: `fddxfsfckyy-172-168-16-65-31003`）
- Loki datasource_id：`1`
- 服务：`winning-winex-ipt-ward-pbc`

### 拉取结果（16:50:32 ~ 17:00:32）
- 第一阶段：40,910 条，耗时 28s
- 第二阶段：5,843 条完整链路，耗时 72s（1 个 trace 超时跳过）
- 总计：46,755 条，899 ERROR，9 个慢接口，35 个代表 traces
- 报告：`logs_20260318_170135_默认集群_report.html`

### environments.json 已更新
- 补入红房子完整配置（tenant_code、grafana url、datasource_id、服务名）

## tool_api 联调结论（17:47）

- **当前状态**：测试环境全流程已打通（getServicesByCodeOrAppid + getServiceLogs + 下载 + 分析 + 报告）
- **模式**：属于传统服务器模式（type: tool_api），不走 Grafana/Loki
- **待办**：工具组部署到生产环境后，需要集成 wxp-tunnel 验证
  - 流程预期：wxp-tunnel 打开运维平台通道 → tool_api 通过通道拉日志
  - 类似乐山 K8s 的链路，但走 tool_api 而不是 Grafana

## 操作规范

### 桐乡市卫生健康局
- 多集群环境（第一/第二/第三集群）
- **拉日志前必须询问用户要哪个集群**，不能自行假设

## 配置策略新决定

用户新确认的环境策略：
- 已知 `k8s` 医院：
  - `乐山市人民医院`
  - `桐乡市卫生健康局`
  - `复旦大学附属妇产科医院(红房子)`
- 后续如果问我拉某医院日志，而我找不到 `31003` 端口，就默认走传统模式

### 后续待办
1. 调整 `skills/log-inspect/config/environments.json`
   - 补入 `复旦大学附属妇产科医院(红房子)` 的配置
   - 固化已知 K8s 医院名单
2. 整理“从自然语言开始的 K8s 日志拉取整体流程”文档
3. 接着把乐山 `15:35 ~ 15:45` 这次全流程跑完，验证：
   - 基本信息里的拉取耗时
   - `be25...` 的时间间隔分析是否恢复
4. 入口路由优化暂缓
   - 当前判断：K8s 已基本打通；traditional/tool_api 测试环境也已打通
   - 说明：测试环境不需要 `wxp-tunnel`，但传统服务器模式真正落到各个现场还需要时间
   - 决定：等后续开始联调传统服务器医院时，再集中优化 `log_inspect_main.py` 的入口路由
   - 重点方向：K8s 失败回退 traditional、医院识别去歧义、tool_api 节点选择策略

## 待办：写日志巡检工作流程文档

新开 session 后立即执行，写入 skills/log-inspect/LOG_INSPECT_WORKFLOW.md

文档要求：
- 以自然语言输入为起点
- 拆分两条业务线：K8s 环境 / 传统服务器（tool_api）
- 每条线详细说明流转流程和规则，类似操作指南
- 内容细化、逻辑清晰

### K8s 业务线要点
- 适用：乐山、桐乡（多集群必须问集群）、红房子
- 流程：wxp-tunnel 打通 Grafana → fetch（Loki 拉 ERROR|业务处理耗时）→ analyze → fetch2（±60s 完整链路）→ re-analyze → report
- 规则：桐乡必须先问集群；二阶段窗口 ±60s；K8s 医院找不到 31003 端口走传统模式

### 传统服务器（tool_api）业务线要点
- 适用：测试医院（工具组测试环境），后续生产环境医院
- 流程：getServicesByCodeOrAppid 查节点 → getServiceLogs 获取下载地址 → 下载 zip → 解压 → preprocess 按时间窗过滤 → analyze → report（无 fetch2）
- 规则：type=ms_all；下载全天日志，preprocess 做时间过滤；生产环境需先 wxp-tunnel 打通运维平台通道
- 待办：工具组部署生产后集成 wxp-tunnel 验证
