# 2026-04-11

## ACP result relay 修复验证通过

### 事情经过
- 第别让我先审阅 `acp-result-relay-fix-report.md`，判断这份修复是否真能解决“child 有结果但 parent 收不到”的 ACP relay 问题
- 先对照 `pi-embedded-DWASRjxE.js` 实际代码，确认报告中的三处补丁都已落地：
  - `captureSubagentCompletionReply`
  - `freezeRunResultAtCompletion`
  - `runSubagentAnnounceFlow`
- 判断修复方向正确：不是盲目延长等待，而是给 relay 增加 transcript 磁盘 fallback 结果来源

### 验证结果
1. **正常场景通过**
   - 使用 `sessions_spawn runtime="acp" agentId="opencode" mode="run"`
   - parent 成功收到 `ACP-RELAY-TEST-OK / child-visible-result`
2. **gateway 重启场景通过**
   - spawn child 后立刻执行 `openclaw gateway restart`
   - parent 仍成功收到 `ACP-RELAY-RESTART-TEST-OK / TOPLEVEL-COUNT=8 / child-result-after-restart`
3. **证据一致**
   - 核对了 stream log 和 child transcript，两边 assistant 输出一致

### 沉淀动作
- 在 `work-system/projects/active/openclaw-tooling/runtime/2026-04-10-acp-result-relay/` 下补了：
  - `acp-result-relay-fix.patch`
  - `validation-2026-04-11.md`
- 更新 `TOOLS.md`，把这处补丁纳入本地 OpenClaw 运维规则
- 更新 `MEMORY.md`，记录 ACP 基础设施现状

### 当前判断
- 本机上的 `ACP -> opencode` 链路，现在不仅 backend 可用，**result relay 也已验证恢复可用**
- 但这仍是 dist 手工补丁，升级 `openclaw` 后大概率会被覆盖，后续必须优先检查
