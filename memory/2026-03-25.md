# 2026-03-25 (Wednesday)

## 门诊护士站 4.2.2 本轮改动收口

今晚围绕 `D:\飞书\download\门诊护士站详细设计文档 (3).docx` 的 `4.2.2` 章节，已先完成一轮口径收敛与改写准备。

### 已确认的新口径

1. **固定资源表无需扩展**
   - 取消原先围绕固定资源的 cron、规则模式、规则版本、自动生成未来可预约数据等设计。
   - 固定资源只保留“后台统一配置的资源定义”角色，用于展示和护士手动新增日程时选择资源。

2. **日程看板已预约数据按预约类型着色**
   - 颜色由本系统后台统一配置维护。
   - 前端按预约类型匹配颜色配置展示，不再依赖旧的泛化口径。

3. **前端组件调用协议需统一落在接口章节**
   - 当前判断最合适放在 `4.2.2.4 模块接口`。
   - 页面说明和流程逻辑章节只保留调用说明，并引用接口章节，不重复展开。

4. **4.2.2.6 时序图需要补齐**
   - 凡涉及核心业务模型落库、状态变化、三方交互的功能，都应补对应业务时序图。

### 已完成的产物

1. **4.2.2 改动骨架**
   - 文件：`work-system/deliverables/4.2.2-预约日程管理-改动骨架-2026-03-25.md`
   - 内容：按 `4.2.2.1 ~ 4.2.2.6` 给出章节级替换建议、删改方向和新增重点。

2. **4.2.2.2 数据设计完整正文**
   - 文件：`work-system/deliverables/4.2.2.2-数据设计-完整正文-2026-03-25.md`
   - 已按新口径重写：
     - 核心数据流转关系
     - 10 张核心表的职责边界
     - 数据清理策略
     - 本轮表结构扩展说明
     - 需删除的旧设计内容
     - 设计结论

3. **核心数据流转图 drawio**
   - 文件：`work-system/deliverables/4.2.2.2-核心数据流转图-2026-03-25.drawio`
   - 已按新版口径重画：
     - 三方推送排班
     - 后台固定资源配置
     - 护士手动新增日程
     - 三方推送预约
     - HIS Check In
     - `outp_schedule_log` 与 `outp_sync_status` 全过程追溯

4. **10 张表的数据结构**
   - 文件：`work-system/deliverables/4.2.2.2-10张表-数据结构-2026-03-25.md`
   - 已按 `4.2.2.2.4.2 outp_schedule_log` 的“字段名 / 类型 / 说明”格式统一展开。

### 本轮识别出的旧口径冲突点

当前原文档中仍残留以下旧设计，需要后续继续清理：
- 固定资源 cron / 定时任务自动生成排班
- `rule_mode / weekly_rule / detail_rule / rule_version` 等扩展字段
- 基于规则变更的排班重建逻辑
- 特殊资源（N 天后日程）设计
- 依赖预生成排班才能新增日程的强约束表述

### 2026-03-25 晚间评审纪要补充

晚间又收到“香港养和门诊综合预约”详设专项评审会议纪要，并已收编进门诊护士站专项。新增确认点包括：
- 当前文档的主要问题不仅在内容缺失，还包括结构混乱、输入输出与模块接口重叠、功能边界定义不清。
- 流程图表达需严格区分“功能模块”和“数据表”，其中数据表要用非矩形图形表示，避免图形语义混乱。
- 当前流程图仍缺少“排班数据推送”等关键流程，且存在错误逻辑连线；其中“手动新增日程是否应更新排班明细状态”仍需进一步确认业务规则。
- 文档表达方式应尽量采用“界面图 + 流程图 + 文字说明”的递进形式，提高可读性。
- 各功能模块的 API 接口定义仍需继续补齐，特别是前后端、三方系统交互所需的入参、出参和回调规则。
- 已新增一个明确时间点：`2026-03-26 13:30` 将与需求侧（Agatha）继续评审，方式为“先演示原型、后讲文档结构”。

### 下次继续时优先动作

建议续做顺序：
1. 继续展开 `4.2.2.4 模块接口`，重点补“前端组件调用协议规范”与完整 API 定义
2. 再补 `4.2.2.6 时序图` 的正式内容或图稿，并注意功能/数据表图形区分规范
3. 复查核心数据流转图，补齐“排班数据推送”等关键流程，并校正可能存在争议的数据流向
4. 如需要，再把 10 张表补成“主键 / 索引 / 关联关系”版本

### 当前状态

本轮属于“文档改写准备 + 关键产物预制完成”，还未正式回贴进原始 docx 文档，但改写正文、图和表结构已经具备继续落文档的基础；同时，专项层面已进一步收敛出明天与需求侧评审前需要重点处理的结构、流程图、接口三类问题。

### 今日收口

今天最终已形成以下关键产物：
- `4.2.2` 改动骨架
- `4.2.2.2` 数据设计完整正文
- 核心数据流转图 `drawio`
- 10 张表的数据结构
- `4.2.2.3 / 4.2.2.4 / 4.2.2.5` 完整正文
- `4.2.2.5` 下 8 个流程的 `PlantUML` 草稿

当前最适合明天继续的主线已经比较明确：先补 `4.2.2.4` 接口细节和 `4.2.2.6` 时序图，再视情况回贴原始文档，准备 `2026-03-26 13:30` 的需求侧评审会。
