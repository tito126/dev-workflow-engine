# 2026-04-13

- 第别在护士站多仓扫描规则上新增明确约束：**如果 YAML 路由/规则文件未配置完成，就不能继续往下推进**。skill 中应明确先让用户维护 YAML，再允许扩扫到第二个仓库。
- 已新增草稿配置文件：`work-system/config/nurse-station-repo-routing.yaml`，包含中文注解、必填项、`status: draft/ready`、白名单扫描根、排除根、关键词路由、validation 规则。当前约束设计为：只有 `status: ready` 且关键 roots 已填、无 TODO 时，才允许多仓扫描。
- 护士站 skill 优化方向已明确为“**配置 + skill 双约束**”，不是只靠记忆：
  - `writing-plans` 应显式带 `repoCandidates / scanRoots` 类信息
  - `locator` 若要从用户给定仓库扩扫到第二仓，必须先读 YAML
  - `findings.md` 必须记录：YAML 路由文件、首扫根、首扫根来源、实际扫描根、被排除的根、扩扫触发原因
- 第别强调：过去“没有大范围扫代码”的良好行为，如果只是建立在记忆系统上，则复用价值不足；应沉淀成可复用的 skill / config 机制。
- 本轮 `1516840-2026-04-12` 的定位与实现过程中，ACP opencode 两次子任务（locator / implementer）都出现“超时 + 无有效输出回传”；这轮**没有再用 `exec opencode` 做降级**。
- 对 `1516840-2026-04-12` 的真实执行归因应谨慎表达：
  - 有效扫描代码、形成定位结论的是主会话手动完成
  - 目标文件里可能出现过 ACP child 留下的初始草稿改动，但没有形成可用回传或结构化结果
  - 最终可交付版本以主会话手动修正、确认并收口后的结果为准
- 阶段状态表达要精确：实现落地 + `implementation-result.md` 已有，只能说进入 **verification-ready / 待验证**，不能说 verification 已完成。
- 已提交一次机制相关提交：`0475640`，提交信息：`Add nurse-station repo routing yaml gate`。
- 后续又统一了 repo-routing 命名：前端根不再叫 `frontend_admin_execution_inpatient`，而改成 `frontend_main -> E:\winning-code\frontend\webui-next`；相关提交包括：`974e0fe`、`ae26669`。
- 已把 YAML 缺失时的默认行为补进 `nurse-station-*`：由 `orchestrator` 自动创建 `draft` 模板 `skills/nurse-station-orchestrator/references/nurse-station-repo-routing.template.yaml`，但创建后必须立刻阻断，等待用户维护 YAML；相关提交包括：`77592c3`。
- 复盘 `1516840` 后进一步沉淀了文案类规则：
  - 产品给了正式原文时，默认视为受保护文本，不允许 implementer 自行改写
  - verification 必须拆分“位置通过”和“文案通过”
  - review-gate 必须能识别“位置可接受但文案不通过”的情况
  - 对应规划文档：`work-system/deliverables/nurse-station/skill-improvement-plan-2026-04-13.md`
- 还补了一条更硬的执行规则：如果 ACP opencode 出现 `backend unavailable`、timeout、empty output、relay 异常等阻塞，必须先报阻塞，等第别确认后才能降级到 `exec opencode`；主会话不得未确认就直接接管代码扫描或改码；相关提交：`fa318d2`。
- `1516840` 的产品反馈已经明确：**位置调整可以接受，但文案必须使用产品原文**。随后已将页面说明文案替换为产品原文，并按参考图调整加粗与颜色；workspace 文档同步修正提交：`d60f692`、`d945eb7`。前端代码仓中的目标文件已改，但当时尚未单独在前端仓提交。
- 对后续工作流的建议已收敛：如果要发起新的需求流程，优先新建 Session，避免当前会话中的框架修订、复盘、规则收紧上下文污染新的需求执行。
