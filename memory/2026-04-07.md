# 2026-04-07

## OpenClaw / OpenCode 执行链验证进展
- 今天围绕护士站框架 v2 的中任务样本 `TFS 1543024`，集中验证了 `wrapper opencode`、`ACP -> OpenCode`、`exec opencode` 三条执行路径。
- 结论已明显收敛：
  - `wrapper opencode` 本次未跑通，不应作为默认方案，也不应作为降级方案。
  - `ACP -> OpenCode` 在 `OpenClaw 2026.4.5` 下已通过**最小连通性验证**，能承接短 prompt + 指定 cwd 的最小任务。
  - 但 `ACP -> OpenCode` 目前**尚未通过正式任务级 prompt 承载验证**；对稍复杂 locate 指令，出现了“请提供具体任务内容 / 请告诉我你想搜索什么”的空回现象。
  - `exec opencode run` 已成功跑通 `1543024` 的 locate-only 实战任务，当前是已验证可用的降级路径。

## 执行路径规则收敛
- 第别今天明确要求：
  - 标准主路径应为 `ACP / acpx -> opencode`
  - 若主路径未通过正式任务级验证，降级方案应是**最原始的 `exec opencode`**
  - `wrapper opencode` 不能定义为降级方案，因为它本次并未跑通
- 这是一个重要边界：以后不能把“未验证成功的实验链路”写进标准执行策略，也不能把它包装成 fallback。

## 提示词层面的重要教训
- 在 `ACP -> OpenCode` locate-only 任务里，今天一度误把“框架元信息”当成“任务正文”塞给 OpenCode，包括：`TFS ID`、`护士站框架 v2`、`requirement-fit checkpoint` 等。
- 第别明确指出：不需要把任务 ID 或框架标签发给 OpenCode，只需要清楚地说“要它做什么”。
- 这个提醒很关键，后续给 OpenCode 的 prompt 应只保留：
  - 在哪个目录
  - 做什么
  - 输出什么格式
  - 禁止什么
- 框架标签、样本标识、工作流元信息应由主控自己维护，不应混入执行器 prompt。

## 1543024 locate-only 成果
- 使用 `exec opencode run` 成功完成 `TFS 1543024` 的窄范围 locate-only。
- 已定位到的关键点：
  - 搜索区主入口：`src/pages/taskOverview/components/homeComps/homeHeader.vue`
  - 已出院方案识别：`type: 'discharged'` + `inpatQuerySchemeId: '399544428'`
  - 查询参数组装核心也在 `homeHeader.vue`
  - 已存在日期相关参数映射：
    - `dischargedFromWardBeginAt / EndAt`
    - `dischargedFromHospitalBeginAt / EndAt`
- 这意味着 `1543024` 很可能不是从零打通新链路，而是把已有参数能力正确暴露到“已出院查询方案”的搜索区。

## 版本与验证结论
- 第别已手动把 OpenClaw 升级到 `2026.4.5`。
- 本轮对 `2026.4.5` 的实际判断应写得很精确：
  - ✅ 已验证：ACP 连 OpenCode 的最小连通性
  - ❌ 尚未验证通过：ACP 对正式任务级 prompt 的稳定承载
  - ✅ 已验证：`exec opencode run` 能稳定完成真实 locate-only 任务

## 1538495 执行与流程规则修正
- 今晚收到新任务：`TFS 1538495`，仓库为 `E:\winning-code\akso5\winning-nis-ward`，要求代码扫描/分析优先走 `ACP -> OpenCode`，模型指定为 `glm-5`。
- 实际执行中：
  - `ACP -> OpenCode` 本次报错 `ACP runtime backend is currently unavailable`，未能跑通。
  - 已按既定降级规则切到 `exec opencode run -m glm-5` 完成 locate-only 分析。
  - TFS 需求信息已确认：`[广州中医药大学第三附属医院][60病区护士]出院通知单增加日间手术患者标志数据`，核心字段为 `INPATIENT_ENCOUNTER.DAY_SURGERY_ADMITTED_FLAG`，值 `98175` 表示日间手术患者。
  - 代码侧已初步确认：打印链路和 encounter 数据链路里已存在 `daySurgeryAdmittedFlag` 字段，问题更像是“出院通知单打印模板/组装逻辑补展示”，不是从零补底层字段。
- 更重要的是，这次暴露出护士站框架的一个流程问题：
  - 我当时直接从 `writing-plans / subagent-execution` 切入，没有先显式过 `nurse-station-brainstorming`。
  - 第别明确纠偏：`nurse-station-*` 后续**应该有顺序**，不能由主控临场跳阶段。
- 需固化的新规则：
  - 默认顺序理解为：`brainstorming -> writing-plans -> subagent-execution / implementer -> verification -> review-gate`
  - 即使用户已给出较完整任务信息，也应先补一个 `brainstorming checkpoint`，显式判断该单是否已完成 intake、目标/边界/成功标准是否足够进入执行。
  - 只有用户明确说“已过 intake，直接进入 planning / execution”时，才允许跳过首段 skill。
- 这条规则后续应作为护士站框架的固定执行约束，而不是临场判断项。
- 已进一步把这条顺序约束沉入相关 skill 文件，避免后续再次只靠聊天记忆执行：
  - `skills/nurse-station-brainstorming/SKILL.md`
  - `skills/nurse-station-writing-plans/SKILL.md`
  - `skills/nurse-station-subagent-execution/SKILL.md`
  - `skills/nurse-station-implementer/SKILL.md`
  - `skills/nurse-station-verification/SKILL.md`
  - `skills/nurse-station-review-gate/SKILL.md`
