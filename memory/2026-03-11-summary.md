# 2026-03-11 日志巡检工具优化总结

## 完成的工作

### 1. API入口提取优化 ✅
**问题**：01c8f97fbb7a47abb7052ff9ecaf99e7 有API入口但没显示
**原因**：之前从最后一条日志提取，但最后一条可能是ERROR（没有API路径）
**修复**：优先从WARN日志中提取API入口，如果没有WARN再从最后一条日志提取

### 2. 高优先级优化（全部完成）✅

#### 2.1 慢接口优化
- **修改**：每个慢接口只取最慢的1个trace（从最多50个减到10个）
- **位置**：`preprocess.py` line 716
- **效果**：减少拉取量，聚焦最典型的问题

#### 2.2 异常分类代表trace拉完整链路
- **修改**：为异常分类的代表traceId拉取完整链路（前后400行上下文）
- **逻辑**：
  - 每个异常分组选1个代表trace（优先选有API入口的）
  - 慢接口10个 + 异常分类31个，共40个代表trace
  - 统一在"链路补强"步骤中拉取
- **位置**：`preprocess.py` line 1168-1293
- **输出示例**：`[链路补强] 慢接口 10 个 + 异常分类 31 个，共 40 个代表 trace...`

#### 2.3 异常影响级别分析
- **新增函数**：`analyze_impact_level(trace_logs)` - 分析trace的影响级别
- **判断逻辑**：
  - 🔴 `high` - HTTP请求线程（`[Jetty-Worker_xxx]`），用户直接感知
  - 🟡 `medium` - RPC调用线程（`[rpc-exec-xxx]`），可能有降级处理
  - 🟢 `low` - 异步任务线程（`[enc-xxx]`），通常不影响主流程
- **排序优化**：按影响级别优先，同级别按trace数量排序
- **位置**：`preprocess.py` line 152-180, line 795-801, line 820-827
- **HTML显示**：`generate_html_report_v2.py` line 156-169

#### 2.4 日志质量分析章节
- **新增函数**：`generate_log_quality_section(log_quality)` - 生成日志质量分析章节
- **统计指标**：
  - 缺少API入口比例（后台任务/异步调用）
  - 异常信息只有null的比例（日志记录不完整）
  - 影响级别分布（高/中/低）
- **位置**：
  - 数据收集：`preprocess.py` line 1296-1312
  - HTML生成：`generate_html_report_v2.py` line 152-203
- **显示效果**：
  - 两个卡片：缺少API入口 / null信息
  - 影响级别分布条（红/黄/绿）

### 3. 最新生产日志分析 ✅
- **时间**：2026-03-11 23:36~23:46（最近10分钟）
- **医院**：乐山市人民医院
- **服务**：病区护士站（winning-winex-ipt-ward-pbc）
- **结果**：
  - 总日志数：29,009
  - ERROR：179
  - WARN：22,147
  - 慢接口：22
- **报告**：`C:\Users\pc\.openclaw\workspace\skills\log-inspect\logs_20260311_234639_默认集群_report.html`

## 待办事项

### 中优先级（下一版本）
1. **多样本迭代优化异常分类**
   - 拉取不同时间段的日志（早高峰、午高峰、晚高峰）
   - 拉取不同医院的日志（乐山、桐乡）
   - 发现新的异常类型，完善分类规则

2. **建立"其他异常"兜底类别**
   - 收集无法归类的错误
   - 定期review发现新模式

3. **从trace的其他日志中提取更多上下文**
   - 如果第一条ERROR只有"null"，使用其他ERROR的内容作为补充

4. **增加"来源类型"字段**
   - 从线程名推断：HTTP请求/RPC调用/后台任务

### 长期优化
1. **分类规则配置化**
   - 将分类规则从代码中抽离到配置文件
   - 每个医院可以自定义规则

2. **日志质量评分机制**
   - API入口覆盖率
   - 详细信息覆盖率
   - 异常分类覆盖率

## 关键技术细节

### 修改的文件
1. `D:\nvm\v24.9.0\node_modules\openclaw\skills\log-inspect\preprocess.py`
   - 慢接口优化（line 716）
   - 链路补强统一处理（line 1168-1293）
   - 影响级别分析（line 152-180）
   - 日志质量统计（line 1296-1312）

2. `D:\nvm\v24.9.0\node_modules\openclaw\skills\log-inspect\generate_html_report_v2.py`
   - 影响级别显示（line 156-169）
   - 日志质量分析章节（line 152-203）

### 新增函数
```python
def analyze_impact_level(trace_logs: list) -> str:
    """分析trace的影响级别（基于线程类型）
    Returns: 'high' | 'medium' | 'low'
    """
    # Jetty-Worker → high
    # rpc-exec → medium
    # enc → low

def generate_log_quality_section(log_quality: dict) -> str:
    """生成日志质量分析章节"""
    # 显示缺少API入口比例、null信息比例、影响级别分布
```

### 关键逻辑
1. **API入口提取**：
   ```python
   # 优先从WARN日志中提取
   for log in sorted_logs:
       if log.get('level') == 'WARN':
           api_match = api_pattern.search(raw_line)
           if api_match:
               api_entry = api_match.group(1)
               break
   ```

2. **链路补强**：
   ```python
   # 慢接口：每个取最慢的1个
   top_slow_trace_ids = set()
   for api in slow_api_summary[:10]:
       for trace in api.get('top_traces', []):  # 只有1个
           top_slow_trace_ids.add(trace['trace_id'])
   
   # 异常分类：每个分组取1个（优先选有API入口的）
   top_error_trace_ids = set()
   for error_group in aggregated_errors:
       samples = error_group.get('samples', [])
       with_api = [s for s in samples if s.get('api_entry') and s['api_entry'] != 'N/A']
       chosen = with_api[0] if with_api else (samples[0] if samples else None)
       if chosen and chosen.get('trace_id'):
           top_error_trace_ids.add(chosen['trace_id'])
   ```

3. **影响级别排序**：
   ```python
   impact_order = {'high': 2, 'medium': 1, 'low': 0}
   sorted(error_groups.items(), key=lambda x: (
       -impact_order.get(x[1].get('impact_level', 'medium'), 1),  # 高影响优先
       -len(x[1]['trace_ids'])  # 同影响级别按数量排序
   ))
   ```

## 验证结果

### 测试数据（2026-03-11 16:22~16:32）
- 总日志数：98,924
- ERROR：1,268
- WARN：92,302
- 慢接口：507
- 链路补强：慢接口10个 + 异常分类31个，共40个代表trace

### 生产数据（2026-03-11 23:36~23:46）
- 总日志数：29,009
- ERROR：179
- WARN：22,147
- 慢接口：22
- 报告：`logs_20260311_234639_默认集群_report.html`

## 下一步计划

1. **新session中继续**：
   - 查看最新生产日志报告
   - 验证4个高优先级优化的效果
   - 根据报告发现的问题，决定下一步优化方向

2. **可能的优化方向**：
   - 如果"其他异常"占比高，需要完善分类规则
   - 如果日志质量问题严重，需要与研发沟通改进
   - 如果影响级别分布不合理，需要调整判断逻辑

## 配置信息

### 乐山市人民医院
- Grafana: http://127.0.0.1:14828
- datasource_id: 2
- app: winning-winex-ipt-ward-pbc
- 服务名：病区护士站

### 桐乡市第一人民医院
- Grafana: http://127.0.0.1:16291
- datasource_id: 2
- app: winning-winex-ward-akso5-pbc
- 服务名：病区护士站

## 技术债务

1. gbk编码问题：emoji在Windows控制台输出时会报错（已知问题，不影响功能）
2. 分类规则硬编码：应该配置化（长期优化）
3. 没有"其他异常"兜底类别：无法归类的错误会被忽略（中优先级）
