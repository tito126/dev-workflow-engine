# 2026-03-12 日志巡检工具 - 代码丢失记录

## 问题
OpenClaw 升级（v2026.2.25 → v2026.3.11）后，workspace 里的文件被覆盖，丢失了今天所有的修改。

## 受影响文件（均在 C:\Users\pc\.openclaw\workspace\skills\log-inspect\）

### generate_html_report_v2.py
**丢失内容：**
- 慢接口详情渲染函数（`generate_slow_api_details` 或类似）
- 显示调用方服务（`caller_display`）
- 显示线程信息
- 显示影响级别
- 基本信息中的拉取时间范围和拉取耗时（今天新加的，已恢复）

**当前状态：**
- 755行，缺少慢接口渲染函数
- 只有 `load_trace_logs` 和部分 error 渲染

### preprocess.py
**丢失内容：**
- `extract_caller_service_from_thread(thread)` 函数
- `categorize_trace()` 中的 caller_service 提取
- `aggregate_errors()` 四级分组键：`category:root_class:api_entry:caller_service`
- `process_file()` 中读取代表trace列表
- `aggregate_errors()` 中代表trace优先逻辑
- `--fetch-start/--fetch-end/--fetch-duration` 参数（今天新加的，已恢复）
- `meta.fetch_range` 写入（今天新加的，已恢复）

### loki_fetcher.py
**丢失内容：**
- `extract_error_representatives()` 重写（按 root_class 分组，而非 error_signature）
- 在日志文件开头写入代表trace列表：`# REPRESENTATIVE_TRACES: trace1,trace2,...`

### log_inspect_main.py
**丢失内容：**
- 去掉 `capture_output=True`（实时显示输出）
- `--max-context-traces 100`
- 记录拉取开始/结束时间（`fetch_start_ts`、`_last_fetch_meta`）
- 传递 `--fetch-start/--fetch-end/--fetch-duration` 给 preprocess.py

## 参考资料

### 旧的工作版本报告
`C:\Users\pc\.openclaw\workspace\skills\log-inspect\logs_20260312_145058_默认集群_report.html`
- 生成时间：2026-03-12 14:50
- 包含完整功能：调用方(30次)、线程(31次)、影响级别(1次)、API入口(25次)

### 旧的工作版本 digest
`C:\Users\pc\.openclaw\workspace\skills\log-inspect\logs_20260312_145058_默认集群_digest.json`
- 可以从这里反推 preprocess.py 的输出格式

## 恢复优先级

1. **generate_html_report_v2.py** - 最高优先级，影响报告展示
   - 恢复慢接口详情渲染
   - 恢复调用方、线程、影响级别显示

2. **preprocess.py** - 高优先级，影响数据分析
   - 恢复四级分组
   - 恢复 caller_service 提取
   - 恢复代表trace优先逻辑

3. **loki_fetcher.py** - 中优先级，影响拉取效率
   - 恢复按 root_class 分组（效率优化）
   - 恢复代表trace列表写入

4. **log_inspect_main.py** - 低优先级
   - 恢复实时输出
   - 恢复 max-context-traces=100

## 恢复方法

从旧报告的 HTML 结构反推 generate_html_report_v2.py 的渲染逻辑。
从旧 digest 的 JSON 结构反推 preprocess.py 的输出格式。

## 关键数据结构（从旧 digest 反推）

error_group 字段：
- category: 错误分类
- root_class: 根本原因类名
- api_entry: API入口
- caller_service: 调用方服务（SELF/winning-winex-xxx/ASYNC/RPC）
- thread: 线程名
- count: trace数量
- samples: 代表trace列表（最多3个）
  - trace_id
  - content
  - caller_service
  - thread

## 今天已完成但需要保留的修改

1. preprocess.py 的 `--fetch-start/--fetch-end/--fetch-duration` 参数
2. preprocess.py 的 `meta.fetch_range` 写入
3. generate_html_report_v2.py 的基本信息表格（拉取时间范围、拉取耗时）
4. log_inspect_main.py 的 `_last_fetch_meta` 和传参逻辑
